spring:
  config:
    import: optional:file:./config/secrets-local.yaml
  datasource:
    url: ${app.db.url}
    driver-class-name: org.h2.Driver
    username: ${app.db.username}
    password: ${app.db.password:}
  h2:
    console:
      enabled: true
      path: /h2-console
